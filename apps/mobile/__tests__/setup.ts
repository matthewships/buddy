// RNTL 14 registers its Jest matchers (toBeOnTheScreen, toBeDisabled, ...)
// from the main entry point; the separate extend-expect import was removed
// in v13, so there is nothing to set up here beyond the preset.
export {};
