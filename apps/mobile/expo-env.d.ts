/// <reference types="expo/types" />

// This file declares the ambient types Expo provides, including the
// side-effect CSS imports NativeWind relies on (`import './global.css'`).
// Expo regenerates it; it is committed so a fresh clone type-checks without
// first running the dev server.
