# Where the web client sends its API requests. Optional: next.config.ts already
# defaults to the deployed Worker for a production build and to the local one
# for `next dev`. Set this to point a build somewhere else.
NEXT_PUBLIC_API_URL=http://localhost:8787
